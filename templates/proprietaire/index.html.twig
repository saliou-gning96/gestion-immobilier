{% extends 'base.html.twig' %}

{% block title %}
  Proprietaire index
{% endblock %}

{% block content %}
  <div class="row">
    <div class="col-sm-12">
      <h4 class="pull-left page-title">
        Proprietaire
      </h4>
      <ol class="breadcrumb pull-right">
        <li><a href="/">Accueil</a></li>
        <li class="active">Proprietaire</li>
      </ol>
    </div>
  </div>

  <div class="row">
    <div class="col-md-12">
      <div class="panel panel-border panel-primary">
        <div class="panel-heading">
          <h3 class="panel-title">
            Liste des proprietaire
          </h3>
        </div>
        <div class="panel-body">
          <div class="row">
            <div class="col-sm-6"></div>
            <div class="col-sm-6">
              <div class="m-b-30 pull-right">
                <a href="{{ path('proprietaire_new') }}"
                  class="btn btn-primary waves-effect waves-light">
                  Créer <i class="fa fa-plus"></i>
                </a>
              </div>
            </div>
          </div>
          <table class="table table-striped table-bordered">
            <thead>
              <tr>
                <th>
                  Id
                </th>
                <th>
                  Prénom
                </th>
                <th>
                  Nom
                </th>
                <th>
                  Adresse
                </th>
                <th>
                  Email
                </th>
                <th>
                  actions
                </th>
              </tr>
            </thead>
            <tbody>
              {% for proprietaire in proprietaires %}
                <tr>
                  <td>
                    {{ proprietaire.id }}
                  </td>
                  <td>
                    {{ proprietaire.user.prenom }}
                  </td>
                  <td>
                    {{ proprietaire.user.nom }}
                  </td>
                  <td>
                    {{ proprietaire.user.adresse }}
                  </td>
                  <td>
                    {{ proprietaire.user.email }}
                  </td>
                  <td>
                    <div class="row">
                      <div class="col-sm-6">
                        <a class="btn btn-success waves-effect waves-light btn-block"
                          href="{{
                          path(
                            'proprietaire_show',
                            {
                              id: proprietaire.id
                            }
                          )
                          }}">
                          Voir
                        </a>
                      </div>
                      <div class="col-sm-6">
                        <a class="btn btn-info waves-effect waves-light btn-block"
                          href="{{
                          path(
                            'proprietaire_edit',
                            {
                              id: proprietaire.id
                            }
                          )
                          }}">
                          Modifier
                        </a>
                      </div>
                    </div>
                  </td>
                </tr>
              {% else %}
                <tr>
                  <td colspan="2">
                    no records found
                  </td>
                </tr>
              {% endfor %}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
{% endblock %}
